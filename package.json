{
  "name": "alps-server-runner-recovery-v1",
  "version": "9.1.8-recovery-v1",
  "description": "ALPS Server Runner Recovery Patch v1: stale-forward detection, persistent paper ledger snapshots, recovery endpoints.",
  "main": "runner.js",
  "scripts": {
    "start": "node runner.js",
    "check": "node --check runner.js"
  },
  "dependencies": {
    "playwright": "^1.48.2"
  },
  "engines": {
    "node": ">=18"
  }
}
